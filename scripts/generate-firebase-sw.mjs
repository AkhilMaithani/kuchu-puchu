import fs from 'node:fs';
import path from 'node:path';
const env=fs.existsSync('.env')?fs.readFileSync('.env','utf8'):'';
const vals={}; for(const line of env.split(/\r?\n/)){const m=line.match(/^([A-Z0-9_]+)=(.*)$/);if(m) vals[m[1]]=m[2].trim().replace(/^['"]|['"]$/g,'');}
const keys=['VITE_FIREBASE_API_KEY','VITE_FIREBASE_AUTH_DOMAIN','VITE_FIREBASE_PROJECT_ID','VITE_FIREBASE_STORAGE_BUCKET','VITE_FIREBASE_MESSAGING_SENDER_ID','VITE_FIREBASE_APP_ID'];
const cfg={apiKey:vals.VITE_FIREBASE_API_KEY||'YOUR_API_KEY',authDomain:vals.VITE_FIREBASE_AUTH_DOMAIN||'YOUR_AUTH_DOMAIN',projectId:vals.VITE_FIREBASE_PROJECT_ID||'YOUR_PROJECT_ID',storageBucket:vals.VITE_FIREBASE_STORAGE_BUCKET||'YOUR_STORAGE_BUCKET',messagingSenderId:vals.VITE_FIREBASE_MESSAGING_SENDER_ID||'YOUR_SENDER_ID',appId:vals.VITE_FIREBASE_APP_ID||'YOUR_APP_ID'};
fs.writeFileSync(path.join('public','firebase-messaging-sw.js'),`importScripts('https://www.gstatic.com/firebasejs/12.1.0/firebase-app-compat.js');\nimportScripts('https://www.gstatic.com/firebasejs/12.1.0/firebase-messaging-compat.js');\nfirebase.initializeApp(${JSON.stringify(cfg)});\nconst messaging=firebase.messaging();\nmessaging.onBackgroundMessage(({notification,data})=>{const n=notification||{}; self.registration.showNotification(n.title||'💌 Friend Reminder',{body:n.body||'You have a reminder.',icon:'/icon-192.png',data:{url:data?.url||'/'}});});\nself.addEventListener('notificationclick',event=>{event.notification.close();event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(cs=>{for(const c of cs){if('focus'in c)return c.focus();}return clients.openWindow(event.notification.data?.url||'/');}));});\n`);
